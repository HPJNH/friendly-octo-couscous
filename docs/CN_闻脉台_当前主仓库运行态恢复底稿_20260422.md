# 闻脉台当前主仓库运行态恢复底稿

日期：2026-04-22  
主仓库路径：`C:\Users\A\Desktop\情报浏览系统\沉香行业情报浏览系统_1.0`

## 1. 恢复来源

本次恢复以已经验证通过的 `2026-04-15` 完整快照为唯一恢复来源：

- `C:\Users\A\Desktop\情报浏览系统\情报浏览系统_整理导出版_20260415`

这份快照此前已单独完成恢复试验，确认：

- 数据库完整可读
- 配置完整
- `data/raw / storage/file_library / data/processed/archive_parsed` 完整
- 首页可打开
- viewer / admin 登录可用
- rebuild 不会归零

## 2. 恢复前备份位置

在对当前主仓库做任何恢复前，先做了外部保护性备份：

- `C:\Users\A\Desktop\情报浏览系统_forensic_backups\wenmai_main_runtime_before_restore_20260422-164927`

另外，在恢复后处理访问码兼容前，又单独备份了一份主仓库数据库：

- `data/database/intelligence_browser_before_access_code_compat_20260422.db`

## 3. 实际恢复了哪些内容

### 已恢复到主仓库正式运行位置

- `config/local_settings.json`
- `data/database/intelligence_browser.db`
- `data/raw`
- `storage/file_library`
- `data/processed/archive_parsed`

### 恢复时做的最小兼容转换

本次不是用旧快照整包覆盖当前代码，只恢复了“运行态数据 + 配置”。  
但旧快照与当前主仓库代码之间存在两个兼容点，因此做了最小修正：

1. 配置兼容
   - 旧快照配置恢复为 `config/local_settings.json`
   - 同时补入当前代码需要的最小兼容字段：
     - `APP_ENV=local`
     - `APP_RUNTIME_ROOT=.`
     - `DATA_ROOT / STORAGE_ROOT / EXPORTS_ROOT / ARCHIVE_ROOT`
     - `APP_SERVER_WORKERS=1`
     - `TRUST_PROXY_HEADERS=false`
   - 旧字段 `EXPORT_ROOT / REPORT_EXPORT_ROOT` 继续保留，避免打断旧导出语义

2. 访问码兼容
   - 旧快照默认访问码是带字母前缀的旧格式
   - 当前主仓库代码已收口为“仅接受 6 位数字访问码”
   - 因此把主仓库中的默认 active 身份做了最小兼容改码：
     - admin：`388588`
     - viewer：`887053`
   - 并同步写回了 `config/local_settings.json`

3. 启动副作用兼容
   - 当前主仓库代码在启动时会执行 `upgrade_existing_documents()`
   - 若不加兼容守卫，会把旧快照库误判成“需要自动重建”，导致 entries 被自动改写
   - 已补最小守卫：只要旧库里已经存在 `entries / sections`，启动时不再自动 full rebuild，也不再自动跑旧文档升级重建

## 4. 恢复后数据库计数

当前主仓库恢复后的数据库计数：

- `entries_all = 251`
- `entries_real = 245`
- `documents = 24`
- `sections = 160`
- `access_identities = 3`

当前 active 访问身份摘要：

- `默认管理员码`：`38***88`，`admin`，`active`
- `默认查看码`：`88***53`，`viewer`，`active`
- 另有 `1` 条 `deleted` viewer 历史身份

结论：

- 当前主仓库数据库已经不是空壳
- 计数与恢复源保持一致量级，没有再次被启动过程改写成空或翻倍

## 5. 首页 / viewer / admin 验证结果

### 首页

- 未登录访问 `/`：`302`
- 正常跳到 `/access/login?next=/`

### viewer 登录

按真实表单流程验证：

- 先 `GET /access/login` 获取 `csrf_token`
- 再 `POST /access/login`

结果：

- viewer 访问码：`887053`
- 最终路径：`/`
- 返回状态：`200`

### admin 登录

按真实表单流程验证：

- 先 `GET /admin/verify` 获取 `csrf_token`
- 再 `POST /admin/verify`

结果：

- admin 访问码：`388588`
- 最终路径：`/upload`
- 返回状态：`200`

### 上传页

- admin 登录后访问 `/upload`
- 返回状态：`200`
- 页面可打开

## 6. 是否已经从“空壳主仓库”恢复成“可信运行态”

结论：**是**

当前主仓库已经从此前的“空壳运行态”恢复为“可信、可运行状态”：

- 配置已回到正式位置
- 数据库已恢复为非空壳
- raw / file library / archive_parsed 已恢复
- 首页可打开
- viewer / admin 可登录
- 上传页可打开
- 关键回归测试仍通过：
  - `python tests/smoke_test.py`
  - `python tests/runtime_config_guard_test.py`
  - `python tests/single_instance_runtime_test.py`

## 7. 当前是否可以进入下一步

结论：**可以进入下一步，但不建议立刻 full rebuild 或部署**

当前更适合的下一步顺序是：

1. 先把当前恢复后的主仓库状态再次冻结一份
2. 再决定是否要做单独的 rebuild 验证
3. rebuild 通过后，再谈部署或同步

当前不建议直接跳到：

- push
- 部署
- 再做目录大清理

因为虽然主仓库运行态已经恢复可信，但仍应先把这次恢复成果固定下来。
