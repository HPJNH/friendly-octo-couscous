# 闻脉台正式访问资格重置说明（2026-04-22）

## 1. 重置目标

本轮将当前系统中的访问资格体系，重置为一组明确、正式、可验证的新身份列表，并清空旧默认身份、旧测试身份、旧临时恢复身份、旧访问码历史和旧失败锁定记录。

## 2. 重置前备份

重置前已备份以下访问资格相关材料：

- 当前 SQLite 数据库副本
- `access_identities`
- `access_code_history`
- `auth_attempts`
- `storage/logs/access_identity_rotation`
- `storage/logs/access_reset_backups`

备份位置：

- `C:\Users\A\Desktop\情报浏览系统_forensic_backups\wenmai_access_formal_reset_20260422-185905`

## 3. 当前正式 active 身份

当前正式 active 身份共 8 个：

- admin：`俊睿`
- admin：`闫斌先生`
- viewer：`赵莹女士`
- viewer：`魏二强先生`
- viewer：`闫力阳先生`
- viewer：`宋庆华先生`
- viewer：`卢亚杰先生`
- viewer：`孟凡让先生`

访问码仅保留在本地安全记录中，不写入仓库文档。

本地安全记录位置：

- `storage/logs/access_identity_rotation/20260422-190100/FORMAL_ACCESS_CODES.txt`

## 4. 已清理的旧访问体系

本轮已清理：

- 旧 default admin / viewer 身份
- 旧测试身份
- 旧临时恢复身份
- 旧 `access_code_history`
- 旧 `auth_attempts`
- 旧 `access_reset_backups` 测试痕迹
- 旧 `access_identity_rotation` 历史轮换记录

同时保留了历史审计日志文本本身，但已解除其与旧 identity 主键的绑定，不再影响当前正式身份体系。

## 5. 验证结果

### 新正式身份

- `俊睿`：admin 验证后进入 `/upload`，状态 `200`
- `闫斌先生`：admin 验证后进入 `/upload`，状态 `200`
- `赵莹女士`：viewer 登录后进入 `/`，状态 `200`
- `魏二强先生`：viewer 登录后进入 `/`，状态 `200`
- `闫力阳先生`：viewer 登录后进入 `/`，状态 `200`
- `宋庆华先生`：viewer 登录后进入 `/`，状态 `200`
- `卢亚杰先生`：viewer 登录后进入 `/`，状态 `200`
- `孟凡让先生`：viewer 登录后进入 `/`，状态 `200`

viewer 登录后访问 `/upload` 会被重定向到 `/admin/verify?next=/upload`，没有越权。

### 旧访问码

以下旧访问码已验证失效：

- 旧默认 admin 配置码
- 旧默认 viewer 配置码
- 旧临时恢复 admin 码
- 旧临时恢复 viewer 码
- 上一轮正式 admin 码
- 上一轮正式 viewer 码

验证结果均回到 `/access/login`，未能再进入系统。

## 6. 当前状态

当前系统已经不再依赖默认身份或临时恢复身份，正式 active 结构为：

- active admin：2
- active viewer：6
- `access_code_history`：仅保留当前正式 8 条
- `auth_attempts`：已清零

## 7. 与部署基线的关系

本轮重置只修改访问资格体系，不修改：

- `entries`
- `documents`
- `sections`
- `entry_marks`
- `raw / file_library / archive_parsed`

因此它可以直接视为当前恢复后运行态上的一次正式访问资格冻结，不影响前面已经完成的 rebuild 验证与部署基线说明。
