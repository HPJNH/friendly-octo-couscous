# 闻脉台收尾状态说明 20260420

## 1. 已完成的包

当前项目已完成以下四个包：

1. 第一包：阅读逻辑重构包
2. 第二包：Parser-A 收口包
3. 第三包：Parser-B 稳态升级包
4. 第四包：豆包提示词切换包

## 2. 第四包完成情况

第四包本次已完成，重点收口如下：

- 正式把 `docx` 写成当前主链路
- 正式把上游输出切到 `v2 9 列`
- 固定 10 个一级标题
- 固定 4 个二级标题
- 明确 `business_tags` 只能使用 machine key
- 明确 `delta_text` 只写本次新增事实
- 明确 `原始链接` 没有就留空，不允许编造
- 明确同一事件持续跟踪时标题必须尽量保持稳定
- 补充了正确示例和错误示例

对应文档：

- `docs/specs/TEMPLATE_SPEC_v2.md`
- `docs/specs/CN_闻脉台_豆包稳定输出指令模板_v2.md`
- `docs/specs/CN_闻脉台_持续跟踪标题规范.md`

## 3. 前三包回归核验结果

### 第一包：阅读逻辑重构包

已确认成立：

- 正式阅读层已采用八大赛道
- 首页快捷视图仍保留
- section 详情页能显示赛道与子类
- 左侧导航仍按“快捷视图 + 八大赛道 + 工具下沉”工作

核验依据：

- `get_day_snapshot('2026-04-12')` 返回 8 个赛道分组
- `get_section_detail('2026-04-12', 'themed_tracks')` 可读到赛道标题和子类
- `tests/smoke_test.py` 通过

### 第二包：Parser-A 收口包

已确认成立：

- v1 / v2 双契约真实兼容
- 新旧二级标题真实兼容
- 显式窗口 metadata 优先
- 原始链接 / 本次新增事实 / 业务标签 已真正落位

核验依据：

- `tests/upload_contract_logic_test.py` 通过
- 代码已落位到 `source_url`、`evidence_json.delta_text`、`evidence_json.business_tags`

### 第三包：Parser-B 稳态升级包

已确认成立：

- `event_anchor / canonical_title / exact-first` 已接入
- `has_meaningful_delta` 已分出 v2 主路径与 v1 fallback
- `why_included` 不再主导误判更新
- `business_tags` 已成为第一分类依据
- safe link 维持 `exact-safe-first`
- fuzzy 主路径默认未重新打开

核验依据：

- `tests/rebuild_engine_stability_test.py` 通过
- 常量中 legacy fuzzy 开关默认关闭

## 4. 当前主程序目录整理结果

当前主目录保留了真正属于程序本体和运行依赖的内容：

- `app/`
- `archive/`
- `config/`
- `data/`
- `deploy/`
- `docs/`
- `exports/`
- `scripts/`
- `storage/`
- `tests/`
- `run.py`
- `requirements.txt`
- `README.md`
- `启动程序.bat`
- `安装依赖.bat`
- `.gitignore`

说明：

- `data/`、`storage/`、`exports/` 仍保留在主目录，是因为它们仍属于当前运行依赖路径或运行输出路径
- 本次没有改动这些路径的程序配置，只对其中历史材料做了归档收拢

## 5. 已归档内容

本次统一归档了以下历史材料：

### 5.1 历史审阅材料

- `docs/review/` 整包
- `docs/GITHUB_REPO_READY.md`
- `docs/KNOWN_ISSUES.md`
- `docs/PROJECT_STRUCTURE.md`
- `archive/legacy_docs/` 中的旧规范与旧代码导出
- `exports/review_packages/` 中的历史 review package
- `exports/情报浏览系统_代码审阅导出_20260416/` 中的历史代码审阅材料

### 5.2 历史研究材料

- 根目录 `深度研究材料与研究说明/` 整包

### 5.3 历史导出物

- `exports/reports/` 中的 rebuild 报告
- `exports/pdf/` 中的导出 PDF
- 历史代码审阅目录中的 PDF

## 6. 归档目录树

当前归档区整理为：

```text
archive/
├── legacy_exports/
│   └── reports_20260420/
├── pdf_exports/
├── research_history/
│   └── 深度研究材料与研究说明/
└── review_history/
    ├── docs_legacy_notes/
    ├── docs_review/
    ├── exports_code_review_20260416/
    ├── exports_review_packages/
    └── legacy_docs/
```

## 7. 当前代码状态

截至本说明生成时：

- 当前分支：`main`
- 当前 commit：`b44ec63de872def98c8216e522be1cb944486d26`
- 当前工作区：存在未提交改动

说明：

- 工作区中的未提交改动不只包含本次收尾文档与归档动作，也包含前三包已经落地但尚未统一提交的代码改动
- 本次没有执行提交、合并、回滚或重置操作

## 8. 当前是否进入可长期维护状态

结论：

- 系统已经进入“可长期维护状态”

原因：

- 前三包的核心结构和契约已经真实落地
- 第四包已把上游模板正式切到当前稳定契约
- 历史审阅材料、旧说明、旧导出物已经统一归档
- 主程序目录已经明显更干净，程序本体与历史材料边界更清楚

仍需注意：

- 当前 Git 工作区仍是未提交状态，后续建议在确认范围后做一次有边界的提交

## 9. 当前明确暂不做的事情

以下内容仍明确不做：

- 数据库结构迁移
- MySQL / Redis / 阿里云
- 新功能扩展
- 前台重写
- parser 主骨架重写
- rebuild 主骨架重写
- fuzzy 主路径重新打开

## 10. 本次收尾结论

本轮已同时完成：

- 前三包回归核验
- 第四包文档收口
- 项目目录归档整理
- 收尾状态说明导出
