# 闻脉台本地运行数据丢失溯源与恢复审阅底稿

日期：2026-04-22  
项目路径：`C:\Users\A\Desktop\情报浏览系统\沉香行业情报浏览系统_1.0`

## 1. 总体判断

- 当前问题更接近“当前项目运行态数据已被替代/清空”，不是单纯“程序读错路径”。
- 这件事已经足以阻塞继续 `push / 部署 / 清理`。在完成数据恢复判断前，不应继续任何发布动作。
- 但这不是“本机所有相关数据都彻底消失”。在项目同级目录仍然发现一份更早的完整快照，可作为第一恢复来源：
  - `C:\Users\A\Desktop\情报浏览系统\情报浏览系统_整理导出版_20260415`
- 当前最合理的判断是：
  - 当前仓库里的 live runtime 已经失真；
  - 但本机仍有较高概率从同级完整快照中恢复到 2026-04-15 的有效状态；
  - 2026-04-15 之后到当前丢失前的增量数据，是否还能找回，仍需继续检查其他外部副本。

## 2. 现状清单

### 2.1 当前冻结备份

- 本轮首先做了项目外部保护性备份，避免继续在仓库内递归复制：
  - `C:\Users\A\Desktop\情报浏览系统_forensic_backups\wenmai_runtime_state_20260422-140759`
- 仓库内较早那份法证备份存在递归复制问题，仅可作为辅证，不应视为最权威冻结点：
  - `archive/private_local_only/forensic_backups/runtime_state_20260422-140626`

### 2.2 当前 live 数据库状态

当前程序默认运行路径指向：

- `DATABASE_PATH=C:\Users\A\Desktop\情报浏览系统\沉香行业情报浏览系统_1.0\data\database\intelligence_browser.db`
- `DATA_ROOT=...\data`
- `STORAGE_ROOT=...\storage`
- `EXPORTS_ROOT=...\exports`
- `ARCHIVE_ROOT=...\data\processed\archive_parsed`
- `LOG_ROOT=...\storage\logs`

但当前正式配置文件已经不在位：

- `config/local_settings.json`：不存在
- `archive/private_local_only/config/local_settings.json`：不存在
- 环境变量中也没有提供 `DATABASE_PATH / DATA_ROOT / STORAGE_ROOT / ARCHIVE_ROOT`

这意味着当前程序之所以还能算出运行路径，是因为 `create_app()` 在没有 `local_settings.json` 的情况下回退到了仓库内默认路径。

当前 live 数据库文件：

- 路径：`data/database/intelligence_browser.db`
- 大小：`143,360` 字节
- 创建时间：`2026-04-22 13:54:31`
- 最近修改：`2026-04-22 14:08:28`

当前 live 数据库内容摘要：

- 表：`access_code_history / access_identities / audit_logs / auth_attempts / documents / entries / entry_marks / export_files / rebuild_runs / sections / sqlite_sequence`
- `access_identities = 8`
- `access_code_history = 8`
- `entries = 0`
- `documents = 0`
- `sections = 0`
- `entry_marks = 0`
- `audit_logs = 0`
- `rebuild_runs = 1`

补充说明：

- 当前 live 库不是完全空白，它保存了 8 条新的访问身份。
- 但它已经不是原来的业务运行库，因为核心业务表 `entries / documents / sections` 都已经归零。

### 2.3 当前 private 副本状态

当前 private 副本数据库状态更差：

- `archive/private_local_only/database/intelligence_browser.db`
  - 大小：`0` 字节
  - 创建/修改：`2026-04-22 13:55:14`
- `archive/private_local_only/database/backups/intelligence_browser_backup_20260421-234453.db`
  - 大小：`0` 字节
  - 创建/修改：`2026-04-22 13:55:14`

结论：

- private 副本并没有保存住原运行库；
- 它本身已经在 2026-04-22 13:55 左右变成了零字节文件。

### 2.4 当前 raw / storage / archive 状态

当前仓库下实际运行目录现状：

- `data/raw`：存在，文件数 `0`
- `data/processed/archive_parsed`：存在，文件数 `0`
- `storage/file_library`：存在，文件数 `0`

private 副本现状：

- `archive/private_local_only/runtime_data/raw`：文件数 `0`
- `archive/private_local_only/runtime_data/archive_parsed`：文件数 `0`
- `archive/private_local_only/storage/file_library`：文件数 `0`

说明：

- 当前项目树里，live 与 private 两套运行数据目录都已经被清空。

### 2.5 当前 Git / 工作区状态

当前工作区不是干净状态：

- `M .gitignore`
- `M app/static/css/collaboration.css`
- `M app/templates/_mark_macros.html`
- `?? archive/`
- `?? data/`
- `?? exports/`
- `?? storage/`

这进一步说明：当前仓库处于“恢复/排障中间态”，不适合继续 `push / 部署 / 清理`。

## 3. 证据链

### 3.1 不是单纯路径错配

如果只是路径错配，通常会出现：

- live 路径无内容，但旧路径里还有完整数据；
- 或者 `local_settings.json` 仍指向别处。

当前实际情况不是这样：

- 当前 `local_settings.json` 已缺失，程序只能靠默认路径运行；
- 当前默认路径下确实有一份数据库文件，但它是 2026-04-22 新建的小库；
- private 副本同样已经变成零字节；
- 当前项目树中 `raw / archive_parsed / file_library` 的 live 和 private 目录都为空。

这说明问题不是“程序读错了原数据”，而是“原数据在当前项目树里已经不在了，且部分路径已被新壳库替代”。

### 3.2 最可疑的时间线

按当前还能看到的时间戳，最可疑的链路如下：

1. `2026-04-22 13:54:31`
   - 当前 live 数据库 `data/database/intelligence_browser.db` 被创建。
   - 它体积只有 `143,360` 字节，明显不是原先那种十几 MB 的运行库。

2. `2026-04-22 13:55:14`
   - `archive/private_local_only/database/intelligence_browser.db` 变成 `0` 字节
   - `archive/private_local_only/database/backups/intelligence_browser_backup_20260421-234453.db` 也变成 `0` 字节

3. `2026-04-22 13:56:03`
   - `exports/reports/MIGRATION_REPORT.md` 生成
   - 其中明确写出：当前有效底稿版本数 `0`、保留真实条目 `0`

4. `2026-04-22 13:56:09`
   - `storage/logs/access_reset_backups/20260422_135603/reset_summary.json` 生成
   - 它明确记录：
     - `backup_counts.access_identities = 0`
     - `created_counts.total = 8`
   - 说明当时程序面对的是一份没有访问身份的空运行库，然后人为重建了 8 条访问身份

5. `2026-04-22 14:08:28`
   - 当前 `data/raw / storage/file_library / data/processed/archive_parsed` 目录的最近修改时间集中在这一时刻
   - 但目录内仍无文件，像是经历了一次“创建/覆盖目录结构但没有真正恢复文件”的动作

### 3.3 哪些动作最可疑

结合时间线，最可疑的不是 parser / rebuild 本身，而是“文件移动/恢复链路”：

1. 私密材料隔离动作  
2. 恢复运行态动作  
3. 封箱整理 / Git 元数据修复期间的目录拷贝与恢复动作  

最值得怀疑的具体模式是：

- 原运行数据先被移出当前 live 路径；
- private 副本没有保住有效内容，甚至数据库副本被写成了 `0` 字节；
- 在 `local_settings.json` 缺失后，程序回退到默认路径；
- 默认路径下自动初始化出新的壳库和空目录；
- 之后又在这份壳库上执行了访问资格重置和 0 条目 rebuild。

### 3.4 当前仓库内仍然存在的“误导性痕迹”

这些痕迹会让人误以为“系统只是空了，但也许还在正常运行”：

- live 库里还有 8 条访问身份
- exports/reports 里还有 rebuild 报告
- storage/logs 里还有访问码重置说明

但它们都只是“新壳库生成后的人为补救痕迹”，不能说明原始业务数据还在。

## 4. 恢复可能性

## 4.1 当前真实运行数据是否仍可恢复

- 对“当前仓库中的原运行态”来说：大概率已经不可直接恢复。
- 对“本机整体数据”来说：仍然有较高恢复可能。

## 4.2 当前恢复概率判断

- 恢复到“至少一份可运行历史版本”：高
- 恢复到“2026-04-15 左右的完整业务状态”：高
- 恢复到“丢失前最新状态（可能晚于 2026-04-15）”：中到低，取决于是否还能找到更晚副本

## 4.3 当前最有价值的恢复来源

### 优先级 1：同级完整快照

路径：

- `C:\Users\A\Desktop\情报浏览系统\情报浏览系统_整理导出版_20260415`

这份快照目前是最重要的恢复来源，原因如下：

- `data/database/intelligence_browser.db` 大小 `13,774,848` 字节
- `entries_all = 251`
- `entries_real = 245`
- `sections = 160`
- `documents = 24`
- `section_date_range = 2026-04-02 ~ 2026-04-12`
- `data/raw`：`11` 个文件
- `data/processed/archive_parsed`：`46` 个文件，约 `25.9 MB`
- `storage/file_library`：`24` 个文件
- `exports/pdf`：`4` 个文件
- 还保留着自己的 `config/local_settings.json`

结论：

- 这不是零散材料，而是一份相对完整、能直接支持恢复的旧运行快照。

### 优先级 2：同级完整快照中的原始底稿与文件库

尤其值得保留与优先恢复：

- `data/raw/drafts/*.docx`
- `data/raw/briefs/*.docx`
- `data/raw/linked/带链接.docx`
- `storage/file_library/active/*`
- `storage/file_library/archived/*`
- `data/processed/archive_parsed/**/*.json`

这些文件能在数据库之外，为后续重新恢复 entries / rebuild / 文档链路提供真实源材料。

### 优先级 3：当前仓库的访问重置痕迹

路径：

- `storage/logs/access_reset_backups/20260422_135603`

用途：

- 可恢复访问身份与应急访问说明
- 不能恢复业务条目、底稿、文件库

### 优先级 4：当前仓库 exports/reports

用途：

- 只能证明“丢失发生后，系统曾在 0 条目状态下做过 rebuild”
- 不能恢复业务数据

## 5. 下一步建议

### 建议 1：继续冻结，不要 push / 部署 / 清理

在完成恢复前，不应继续：

- push
- 部署
- 清理目录
- 再做交付整理

因为当前 live 仓库已经不是可信运行态。

### 建议 2：先基于同级完整快照做“恢复试验副本”

不要直接覆盖当前仓库。更稳的做法是：

1. 把 `情报浏览系统_整理导出版_20260415` 复制到新的恢复工作目录
2. 在那个恢复工作目录里验证：
   - 数据库可读
   - raw / archive_parsed / file_library 完整
   - 首页可打开
   - rebuild 不会再归零

### 建议 3：如果要恢复当前仓库，也应先恢复配置与运行数据，再碰代码

恢复顺序建议：

1. 恢复 `config/local_settings.json`
2. 恢复 `data/database/intelligence_browser.db`
3. 恢复 `data/raw`
4. 恢复 `storage/file_library`
5. 恢复 `data/processed/archive_parsed`
6. 最后再验证首页、登录、rebuild

### 建议 4：继续寻找 2026-04-15 之后的更晚副本

当前最完整的已知来源是 2026-04-15 快照，但它未覆盖 2026-04-20 / 2026-04-21 附近的最新状态。  
如果还要追求“丢失前最新状态”，最值得继续查的方向是：

1. 本机其他同级或上级目录中的旧项目副本
2. Windows 历史版本 / 云同步目录 / 另一台机器
3. 任何未被纳入当前仓库的外部数据库拷贝

## 6. 当前结论

- 当前问题不是简单路径错配，而是当前项目运行态已经被壳库和空目录替代。
- 当前仓库内的 live runtime 不能再被视为“可信最新状态”。
- 当前最值得优先检查并用于恢复的来源，是：
  - `C:\Users\A\Desktop\情报浏览系统\情报浏览系统_整理导出版_20260415`
- 在完成恢复判断前，不应继续 push / 部署 / 清理。
