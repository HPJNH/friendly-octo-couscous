# 闻脉台恢复成果冻结说明

日期：2026-04-22

## 1. 当前冻结时间

- 冻结时间：2026-04-22 17:07:44

## 2. 当前主仓库路径

- `C:\Users\A\Desktop\情报浏览系统\沉香行业情报浏览系统_1.0`

## 3. 当前恢复来源

本次恢复成果基于已验证可信的恢复来源：

- `C:\Users\A\Desktop\情报浏览系统\情报浏览系统_整理导出版_20260415`

恢复原则：

- 保留当前主仓库代码
- 不用旧快照整包覆盖当前主仓库代码
- 只恢复运行态数据与配置

## 4. 当前数据库核心计数

当前冻结时主仓库数据库：

- 路径：`data/database/intelligence_browser.db`

核心计数如下：

- `entries_all = 548`
- `entries_real = 526`
- `documents = 26`
- `sections = 180`
- `access_identities = 3`

补充说明：

- 当前数据库已经明确不是空壳库
- 这里冻结的是“恢复成功后的运行态节点”，当时仍使用一组临时/默认身份做访问验证
- 该身份状态已于 `2026-04-22` 正式访问资格重置后失效，不再代表当前正式访问体系
- 当前正式身份请以 [CN_闻脉台_正式访问资格重置说明_20260422.md](C:/Users/A/Desktop/情报浏览系统/沉香行业情报浏览系统_1.0/docs/CN_闻脉台_正式访问资格重置说明_20260422.md) 为准

## 5. 当前登录验证结果

按真实表单流程（含 CSRF token）验证：

### 首页

- 未登录访问 `/`：`302`
- 正常跳转到 `/access/login?next=/`

### viewer

- 当时使用一组临时恢复 viewer 访问码
- 登录后最终路径：`/`
- 返回状态：`200`

### admin

- 当时使用一组临时恢复 admin 访问码
- 登录后最终路径：`/upload`
- 返回状态：`200`

### upload

- admin 登录后访问 `/upload`
- 返回状态：`200`
- 页面可打开

## 6. 当前恢复后的关键目录状态

### `data/raw`

- 文件数：`11`
- 总体积：`688,485` 字节

### `storage/file_library`

- 文件数：`26`
- 总体积：`1,606,498` 字节

### `data/processed/archive_parsed`

- 文件数：`58`
- 总体积：`31,332,820` 字节

## 7. 当前为什么可以视为“恢复成功后的可信节点”

可以把当前状态视为“恢复成功后的可信节点”，原因是：

1. 配置已回到正式位置：
   - `config/local_settings.json`

2. 当前数据库已恢复为非空壳，并且业务表已有明确内容：
   - entries / documents / sections 均非零

3. 首页、viewer、admin、upload 链路均已验证通过

4. 关键运行目录已恢复为非空：
   - raw
   - file_library
   - archive_parsed

5. 关键回归测试通过：
   - `python tests/smoke_test.py`
   - `python tests/runtime_config_guard_test.py`
   - `python tests/single_instance_runtime_test.py`

6. 当前状态已做外部冻结备份，可回退：
   - `C:\Users\A\Desktop\情报浏览系统_forensic_backups\wenmai_restored_state_final_freeze_20260422-170744`

## 8. 下一步建议只剩什么

当前冻结后的下一步，只建议做两类事情：

1. 单独做 rebuild 验证
   - 作为独立一包执行
   - 不与部署、同步、目录整理混做

2. 部署前准备
   - 但应在 rebuild 验证完成后再考虑
   - 当前不建议立刻 push、部署或继续大范围清理

## 9. 当前冻结结论

- 当前恢复成果已经冻结完成
- 当前可以作为后续“单独 rebuild 验证包”的安全基线
- 当前仍建议保持：
  - 不部署
  - 不 push
  - 不继续 full rebuild
